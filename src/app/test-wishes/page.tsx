import Wishes from "@/components/features/animation/wishes";

export default function TestWishesPage() {
    return (
        <main className="min-h-screen flex items-center justify-center">
            <Wishes />
        </main>
    );
}